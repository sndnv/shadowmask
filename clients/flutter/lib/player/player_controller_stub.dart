import 'package:shadowmask/player/player_controller.dart';

Future<void> preparePlayer() async {}

PlayerController makePlayerController(String baseUrl) =>
    throw UnsupportedError('The video player is not available here.');
