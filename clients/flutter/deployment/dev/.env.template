# Notes:
#  - For development and testing, copy this file to `web/assets/.env` and adjust the values
#  - For production, `deployment/production/entrypoint.sh` renders `deployment/production/.env.template`
#    into the same location at container start
#  - A `--dart-define=SHADOWMASK_API_BASE=...` passed to `flutter run` or `flutter build` takes
#    precedence over this file

SHADOWMASK_API_BASE=http://localhost:8080
